import 'package:flutter/material.dart';

class EmergencyWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("EmergencyWidget"),
      ),
    );
  }
}
