import 'package:flutter/material.dart';

class PreventionWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("PreventionWidget"),
      ),
    );
  }
}
